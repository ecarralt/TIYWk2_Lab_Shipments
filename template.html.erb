<!DOCTYPE html>
<html>
  <head>

    <link rel="stylesheet" href="normalize.css">
    <link rel="stylesheet" href="styles.css">

    <meta charset="utf-8">
    <title>Weekly Shipments</title>

    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
      google.charts.load('current', {'packages':['corechart']});
      google.charts.setOnLoadCallback(drawChart);
      function drawChart() {

        var data = google.visualization.arrayToDataTable([
          ['Employee', 'Money'],
          ['Fry', <%= fry_money%> ],
          ['Amy',  <%= amy_money%>],
          ['Bender',  <%= bender_money%>],
          ['Leela', <%= leela_money%>],
        ]);

        var options = {
          title: 'Money Generated by Employee',
          backgroundColor: '#ecf0f1',
          // titleTextStyle{fontSize: <25>}
        };


        var chart = new google.visualization.PieChart(document.getElementById('piechart'));

        chart.draw(data, options);
      }
    </script>

  </head>
  <body>
    <h1>This Week's Total is:  <%= as_money(total_money) %> </h1>
    <div class="tables">
      <div class= "shipdata_block">
        <h2>Weekly Shipment Data</h2>

        <table border="1">
          <tr>
            <th>Destination</th>
            <th>Shipment</th>
            <th>Crates</th>
            <th>Money</th>
          </tr>
          <% shipment_data.each do |shipment| %>
            <tr>
              <td><%= shipment["Destination"]%></td>
              <td><%= shipment["Shipment"]%></td>
              <td><%= shipment["Crates"]%></td>
              <td><%= as_money(shipment["Money"]) %></td>
            </tr>
          <%end%>
        </table>
      </div>

      <div class="empldata_block">
        <h2>Weekly Employee Data</h2>

        <table border="1">
          <tr>
            <th>Employee</th>
            <th>Trips</th>
            <th>Bonus</th>
          </tr>
          <% employee_data.each do |employee| %>
            <tr>
              <td><%= employee[:employee]%></td>
              <td><%= employee[:trips]%></td>
              <td><%= as_money(employee[:bonus])%></td>
            </tr>
          <%end%>
        </table>
      </div>
    </div>

    <div id="piechart" style="width: 900px; height: 500px;"></div>
    <img src="https://media.giphy.com/media/26vUvUqJSc6wESLwQ/giphy.gif">
    <p>Copyright © Enrique Carral Trevino</p>
  </body>
</html>
